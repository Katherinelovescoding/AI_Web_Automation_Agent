# AI provider (required for the agent)
GOOGLE_GENERATIVE_AI_API_KEY=your_google_gemini_api_key

# Target form URL — the page the agent will navigate to and fill out (optional: defaults to https://example.com)
# TARGET_URL=https://your-form-page.example.com/
